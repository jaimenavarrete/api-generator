import _pool from '../common/database/postgresqlConnection';

export const getResourceItems = async (
    apiSlug: string,
    resourceSlug: string
) => {
    console.log(apiSlug);
    console.log(resourceSlug);
};
